# دستورالعمل اجرایی پروژه

برای هر طراحی، پیاده‌سازی، بازبینی یا تغییر در این workspace، فایل
`docs/00-documentation-standard.md` را پیش از اقدام بخوان و قواعد آن را رعایت کن.

این سند مرجع الزام‌آورِ مستندسازی، طراحی UI/UX و کدنویسی React پروژه است. قواعد
آن بر الگوهای عمومی، پیشنهادهای ابزارها و تصمیم‌های سلیقه‌ای تقدم دارند؛ مگر آنکه
کاربر صریحاً استثنا یا تغییر آن را درخواست کند.

پروژه فقط مصرف‌کنندهٔ APIهای موجود است. پیاده‌سازی یا تغییر Backend خارج از scope
است؛ ابهام یا کمبود قرارداد API باید مستند و به تیم Backend ارجاع شود، نه اینکه در
رابط کاربری حدس زده یا جبران شود.
