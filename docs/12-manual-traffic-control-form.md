# مستند کامل فرم کنترل تردد دستی

## وضعیت سند

| مورد | مقدار |
|---|---|
| فرم | `ManualTrafficControlForm` |
| عنوان نمایشی | کنترل تردد |
| وضعیت | رفتار WinForms مستند؛ قرارداد Backend باید به‌صورت OpenAPI نسخه‌دار تحویل شود؛ معماری وب/سرویس تجهیزات مستند |
| منبع اصلی | `EosForms/ManualTrafficControlForm.cs` و `ManualTrafficControlForm.Designer.cs` |
| جایگاه | مرکز عملیاتی ورود، خروج، پرداخت و کنترل تجهیزات |

برچسب «رفتار فعلی» از کد و Designer استخراج شده است. برچسب «پیشنهاد نسخه وب» تصمیم معماری برای نسخه جدید است و نباید رفتار فعلی تلقی شود.

## ۱. هدف و جایگاه فرم

فرم برای کنترل لحظه‌ای ورود و خروج خودرو در یک درب مشخص و برای شیفت کاربر طراحی شده است. فرم هم‌زمان نقش‌های زیر را دارد:

- دریافت شناسه خودرو از اپراتور، کارت، QR، UHF و دوربین پلاک‌خوان؛
- یافتن عضو، خودرو و تردد باز؛
- اجرای قواعد عضویت، تعرفه و فهرست‌های کنترلی؛
- ثبت ورود و خروج؛
- محاسبه و دریافت پرداخت؛
- چاپ قبض؛
- ارسال فرمان به راهبند؛
- نمایش ترددهای اخیر و وضعیت تجهیزات؛
- ثبت بار ورودی یا خروجی برای تردد انتخاب‌شده.

## ۲. مسیر دسترسی و پیش‌شرط‌ها

### رفتار فعلی

فرم از `MainForm` و پس از انتخاب پارکینگ باز می‌شود. در زمان نمایش:

1. نشست کاربر refresh می‌شود.
2. پارکینگ فرم با `CurrentUser.CurrentParking` مقایسه می‌شود.
3. درب متناظر با `CurrentUser.DoorShift` از `ParkingApi.GetParkingDoor` دریافت می‌شود.
4. نوع درب، نوع مجوز ورود و تجهیزات درب روی UI اعمال می‌شود.
5. تعرفه‌ها و ترددهای اخیر بارگذاری می‌شوند.

در صورت نبودن پارکینگ جاری یا درب/شیفت معتبر، پیام خطا نمایش داده شده و فرم بسته می‌شود.

### نقش و مجوز

مجوزهای مشاهده‌شده در کد:

| مجوز/شرط | اثر |
|---|---|
| `ParkingManualIOEditCarInfo` | فعال یا غیرفعال‌کردن پنل اطلاعات خودرو |
| `parkingManualIOBillControl` | نمایش کنترل صدور فیش کنترلی |
| `ParkingManualIOSetTarrif` | امکان جابه‌جایی تعرفه با میانبر Ctrl+W/Ctrl+Q |
| `LockOprator.HasAccessToCargoAbility` | نمایش عملیات ثبت بار |
| `ExitPermission` در تنظیمات | فعال‌شدن بررسی مجوز خروج |

ثبت نهایی نام مجوزها، کد عددی و تفکیک `hidden`، `disabled` و `unauthorized` نیازمند تأیید رجیستری رسمی مجوزهاست.

## ۳. ساختار کامل UI

### ۳.۱ سربرگ و اطلاعات context

`infoLabel` اطلاعات زیر را نمایش می‌دهد:

- نام کاربر جاری؛
- عنوان درب جاری؛
- نوع درب؛
- تعرفه جاری؛
- زمان آخرین بروزرسانی فهرست تردد.

`doorStateLabel` وضعیت فرمان راهبند را نمایش می‌دهد؛ مانند «درب باز شد»، «درب باز نشد»، «رله یافت نشد» یا «ارسال دستور به راهبند».

### ۳.۲ پنل شناسایی خودرو

| کنترل | نوع | رفتار |
|---|---|---|
| `eosPlateControl1` | کنترل پلاک | ورود دستی، دریافت پلاک از دوربین و اعتبارسنجی پلاک |
| `cardTextBox` | ورودی کارت | دریافت کارت، UHF یا QR و شروع جست‌وجو پس از validation |
| `memberCodeTextBox` | ورودی کد عضو | جست‌وجوی عضو و خودروهای وابسته |
| `tariffTextBox` | انتخاب تعرفه | نمایش تعرفه عمومی یا تعرفه عضو |
| `carPicture` | تصویر | نمایش تصویر ورود خودرو |
| `MemberpictureBox` | نشان عضو | با شناسایی عضو فعال می‌شود |
| `grbMemberInfo` | مشخصات عضو | نام، نام خانوادگی، کد ملی و تصویر شخصی |
| `additionalInfoLabel` | پیام تکمیلی | اعتبار عضو، پیام‌های دستگاه یا وضعیت جانبی |
| `enterDateLabel` | زمان ورود | نمایش زمان ورود ثبت‌شده یا خطای نبود ورود |

### ۳.۳ دکمه‌های اصلی

| دکمه | متن | رفتار |
|---|---|---|
| `carEnterButton` | ورود | اعتبارسنجی و ثبت ورود از طریق `CreateEnterDump` |
| `carExitButton` | خروج | اعتبارسنجی، محاسبه صورتحساب، پرداخت و ثبت خروج |
| `carSearchButton` | جستجو | بازکردن `ManageTrafficRecordForm` برای جست‌وجوی تردد |
| `cancelButton` | لغو | پاک‌کردن اطلاعات جاری، تصویر و توقف پردازش خودکار |
| `settingButton` | تنظیمات | نمایش یا مخفی‌کردن تنظیمات محلی فرم |
| `btnReloadCamera` | بدون نمایش پیش‌فرض | راه‌اندازی مجدد دوربین‌ها در صورت فعال‌شدن |
| `platelistButton` | بدون نمایش پیش‌فرض | نمایش فهرست پلاک‌های تشخیص‌داده‌شده |
| `simpleButton1` | Minimize | کمینه‌کردن همه پنجره‌ها؛ در حالت عادی مخفی است |
| `plateDetectedButton` | کنترل آزمایشی/پلاک تشخیص‌شده | نمایش پلاک تشخیص‌داده‌شده؛ در Designer مخفی است |

### ۳.۴ کنترل‌های ورود و خروج

| کنترل | متن/کاربرد |
|---|---|
| `controlBillCheckBox` | صدور فیش کنترلی دو نسخه‌ای؛ فقط با شرط پارکینگ و مجوز قابل مشاهده است |
| `entranceTypeComboBox` | رفتار ورود خودکار/دستی |
| `exitTypeComboBox` | رفتار خروج خودکار/دستی |
| `entrancePrintCheckBox` | چاپ فیش ورودی، مخصوصاً مسیر QR |
| `exitPrintCheckBox` | چاپ فیش خروج |
| `checkBoxTrafficMustHavePlate` | اجباری‌کردن ثبت پلاک برای ورود |
| `checkBoxExitWithoutEntrance` | اجازه خروج بدون ورودی |
| `checkBoxEnterWithoutBeforeExit` | اجازه ورود چندباره |
| `onlyMemberheckBox` | محدودکردن تردد به اعضا |
| `checkBoxCheckRepeatedTrade` | بررسی تردد تکراری در فهرست جاری |
| `repeatedRecordTimeUpDown` | بازه زمانی تشخیص تردد تکراری |
| `checkBoxDontShowPayFormNotMembers` | عدم نمایش فرم پرداخت برای غیرعضو |
| `checkBoxDontCheckPlateInCurrentList` | کنترل فهرست جاری پلاک در دوربین؛ مقدار آن به تنظیمات هر دو دوربین منتقل می‌شود |
| `relayBuzzerCheckBox` | فعال‌کردن صدای بوق هنگام فرمان راهبند |
| `checkBoxG4FastCommand` | استفاده از مسیر سریع فرمان G4 |
| `checkBoxIgnoreRepeatedCardID` | نادیده‌گرفتن کارت تکراری در بازه مشخص |
| `numericUpDownIgnoreCardIDTime` | بازه زمانی نادیده‌گرفتن کارت تکراری |

مقادیر پیش‌فرض Designer که باید در طراحی وب تعیین تکلیف شوند:

- «عدم نمایش گزارش خروج غیر عضو» فعال است؛
- «عدم وجود پلاک در لیست جاری» فعال است؛
- «دستور سریع به راهبند» فعال است؛
- «صدای بوق رله» فعال است؛
- نوع ورود و خروج پیش‌فرض «غیر خودکار» است؛
- کنترل‌های تشخیص پلاک مستقل و دکمه فهرست پلاک در Designer مخفی‌اند؛
- کنترل قبض کنترلی در ابتدا مخفی است و فقط با نوع درب، تنظیم پارکینگ و مجوز ظاهر می‌شود.

### ۳.۵ دوربین‌ها

دو تب وجود دارد:

- تب «دوربین ورودی» با `eosIpCamView1`؛
- تب «دوربین خروجی» با `eosExitIpCamView2`.

هر دوربین منوی «برقراری ارتباط» دارد. دوربین ورود تصویر و پلاک را برای ورود تأمین می‌کند و دوربین خروج با تعیین `recivedPlateFromExitCamera` رفتار خودکار خروج یا ورود را تغییر می‌دهد.

### ۳.۶ جدول ریز تردد

گروه `groupControl1` با عنوان «ریز تردد» شامل `trafficGrid` است.

ستون‌ها و عملیات:

- کد عضو؛
- پلاک؛
- نام کامل عضو؛
- زمان ورود؛
- زمان خروج؛
- نوع پرداخت؛
- مبلغ؛
- عملیات پرداخت؛
- عملیات خروج؛
- عملیات ثبت بار.

عملیات هر ردیف:

| عملیات | شرط | اثر |
|---|---|---|
| پرداخت | خودرو خروج ثبت شده و پرداخت نشده باشد | بازکردن `PayForm` |
| خروج | خروج ثبت نشده باشد | انتقال مشخصات ردیف به فرم اصلی |
| ثبت بار | مجوز قابلیت بار فعال باشد | نمایش منوی بار ورودی/خروجی |

زمان‌ها در Grid به تاریخ شمسی تبدیل می‌شوند و ردیف‌های دارای خروج با رنگ متفاوت نمایش داده می‌شوند.

### ۳.۷ اطلاعات خودرو

`infoGridControl` در گروه «مشخصات وسیله نقلیه» نمایش داده می‌شود:

- زمان ورود؛
- نام خودرو؛
- نوع خودرو؛
- رنگ؛
- مدل؛
- نام عضو.

ویرایش این بخش به مجوز `ParkingManualIOEditCarInfo` وابسته است.

### ۳.۸ وضعیت دستگاه‌ها

`deviceGridControl` در گروه «وضعیت دستگاه‌ها» این اطلاعات را نشان می‌دهد:

- نام دستگاه؛
- نسخه firmware؛
- زمان جاری دستگاه؛
- نوع ترافیک/کاربری دستگاه؛
- عملیات تنظیم ساعت.

رنگ ردیف‌ها وضعیت دستگاه را نشان می‌دهد: سبز برای وضعیت سالم، نارنجی برای اختلاف زمانی و قرمز برای قطع ارتباط یا نبود داده معتبر.

دکمه تنظیم ساعت، زمان دستگاه را با زمان سیستم هماهنگ می‌کند؛ برای راهبند ممکن است به‌جای تنظیم ساعت، فرمان تست/بازکردن ارسال شود.

ساختار Layout در Designer شامل `splitContainerControl1`، `splitContainer1`، `gridSplitContainer1`، `tableLayoutPanel1`، `flowLayoutPanel1` و پنل‌های `panel1` تا `panel6` است. این اجزا رفتار کسب‌وکاری مستقلی ندارند و فقط برای چیدمان استفاده می‌شوند. `groupControl1` عنوان «ریز تردد»، `groupControl2` عنوان «وضعیت دستگاه‌ها» و `groupBox1` عنوان «مشخصات وسیله نقلیه» را تأمین می‌کنند.

دکمه‌های عملیاتی Grid عبارت‌اند از: `payRepositoryGridButton` برای پرداخت، `exitRepositoryGridButton` برای انتقال یک تردد باز به فرم کنترل، `cargoRepositoryGridButton` برای ثبت بار و `syncTimeDeviceRepositoryButtonEdit` برای تنظیم ساعت یا تست تجهیزات.

### ۳.۹ کنترل قبض اولیه و انتخاب خودرو

- `BillCheckerControl`: دریافت و بررسی شناسه‌های قبض اولیه پیش از خروج.
- `EosPlateSelector`: انتخاب خودرو زمانی که یک عضو چند خودرو دارد.
- `cargoMenuStrip`: شامل «ثبت بار ورودی» و «ثبت بار خروجی».

برای POS، پاسخ خام در `PcPosResponseReady` parse می‌شود و به `PayForm` ارسال می‌گردد؛ در نسخه وب این مسیر باید از UI خارج و از طریق Backend/Windows Service مدیریت شود.

### ۳.۱۰ کنترل‌ها و handlerهای پنهان یا آزمایشی

این موارد در کد یا Designer وجود دارند ولی بخشی از مسیر عادی کاربر نیستند:

- `textBox1` و `button1`: ورودی آزمایشی کارت؛ هر دو مخفی‌اند.
- `plateDetectedButton`: نمایش/آزمایش پلاک تشخیص‌داده‌شده؛ مخفی است.
- `plateDetectCheckBox`: فعال‌سازی Plate Detector خارجی؛ مخفی است و فقط در صورت وجود تنظیمات detector قابل استفاده است.
- `platelistButton`: بازکردن `PlateDetectedList`؛ پس از بازکردن پنجره غیرفعال می‌شود.
- `labelRepeatedPlate`: پلاک تکراری دوربین را نشان می‌دهد و با کلیک همان پلاک را وارد فرم می‌کند.
- `labelRepeatedCardID`: کارت تکراری را نشان می‌دهد و با کلیک آن را وارد و validate می‌کند.
- منوی context هر دوربین و گزینه «برقراری ارتباط»، `StartCamera` را اجرا می‌کند.
- `btnReloadCamera` نیز `StartCamera` را دوباره اجرا می‌کند و در حالت عادی مخفی است.
- چند handler مانند `cameraPauseButton_Click`، `simpleButton2_Click` و handlerهای Opening فعلاً بدنه عملیاتی ندارند و نباید قابلیت محصولی قطعی تلقی شوند.

## ۴. حالت‌های فرم

| حالت | مشخصات |
|---|---|
| آماده‌سازی | فرم باز است ولی درب یا تجهیزات هنوز کامل نشده‌اند |
| آماده | درب، تعرفه و تجهیزات قابل استفاده‌اند |
| انتظار شناسه | فرم پلاک، کارت یا کد عضو را دریافت می‌کند |
| اطلاعات یافت شد | اطلاعات خودرو/عضو و دکمه‌های مجاز نمایش داده می‌شوند |
| انتخاب خودرو | عضو چند خودرو دارد و باید یک خودرو انتخاب شود |
| در حال ثبت | دکمه ورود/خروج غیرفعال و درخواست در حال اجراست |
| انتظار پرداخت | صورتحساب خروج دریافت شده و پرداخت باید تأیید شود |
| انتظار قبض اولیه | شناسه‌های قبض اولیه باید وارد یا تأیید شوند |
| خطای دستگاه | اتصال دوربین، کارت‌خوان یا راهبند ناموفق است |
| لغوشده | اطلاعات خودرو، تصویر و ورودی‌های موقت پاک شده‌اند |
| بستن | Timer، eventها و اتصالات تجهیزات آزاد می‌شوند |

چرخه عمر دقیق فرم:

- Constructor پارکینگ و تنظیمات `ClearFormMinute` و `UseSimilarPlateForMember` را می‌خواند و بارگذاری تجهیزات را آغاز می‌کند.
- `Load` تنظیمات محلی را بارگذاری می‌کند.
- `Shown` نشست، پارکینگ، شیفت، درب، تعرفه و Timer را آماده می‌کند.
- Timer ترددهای اخیر را در Thread جداگانه دریافت می‌کند.
- `FormClosing` تنظیمات را ذخیره و اتصال کارت‌خوان، UHF، StPro، راهبندها و POS را آزاد می‌کند.
- `FormClosed` handler دارد ولی منطق مستقل قابل توجهی ندارد.
- `CancelForm` state خودرو، تصویر، پیام راهبند و انتظار دوربین‌ها را پاک می‌کند.

## ۵. جریان شناسایی خودرو

1. کاربر پلاک، کارت یا کد عضو را وارد می‌کند.
2. رویداد `Validated` اجرا می‌شود.
3. در حالت کارت و پلاک، هر دو مقدار اجباری می‌شوند.
4. تکراری‌بودن تردد بررسی می‌شود.
5. `TrafficApi.GetCarTrafficInfo` فراخوانی می‌شود.
6. نتیجه در `SetFormCarInfo` به state و UI تبدیل می‌شود.
7. تعرفه عضو، تصویر ورود، وضعیت ورود و اطلاعات عضو نمایش داده می‌شوند.
8. بر اساس تنظیمات، ورود یا خروج خودکار ممکن است اجرا شود.

منابع شناسایی:

- ورود دستی پلاک؛
- event پلاک دوربین؛
- event کارت دستگاه؛
- UHF؛
- QR رمزگشایی‌شده؛
- انتخاب از جدول تردد؛
- جست‌وجو در فرم مدیریت تردد.

## ۶. جریان ثبت ورود

در `CreateEnterDump`:

1. تصویر خودرو گرفته یا از تصویر پلاک‌خوان استفاده می‌شود.
2. نوع مجوز ورود بررسی می‌شود.
3. کارت، پلاک یا تصویر اجباری کنترل می‌شود.
4. فهرست کنترلی بررسی می‌شود.
5. `TrafficDumpEntity` ساخته می‌شود.
6. `DoorId`، `CardId`، `MemberId`، پلاک، خودرو، تعرفه، تصویر، کاربر و نوع منبع ثبت می‌شوند.
7. در صورت فعال‌بودن قبض کنترلی، `BillControlType` تنظیم می‌شود.
8. `TrafficApi.CreateEnterDump` فراخوانی می‌شود.
9. در صورت موفقیت، قبض لازم چاپ، راهبند باز و فرم پاک می‌شود.

## ۷. جریان ثبت خروج و پرداخت

در `DoExitDump`:

1. مجوز خروج اختیاری بررسی می‌شود.
2. تصویر خروج گرفته می‌شود.
3. الزامات کارت، پلاک و تصویر بررسی می‌شوند.
4. لیست کنترلی بررسی می‌شود.
5. وجود ورود قبلی کنترل می‌شود. **تصمیم محصول برای نسخه وب:** اگر ورود پیدا نشود، workflow خروج متوقف و `ManageTrafficRecordForm`/صفحه مدیریت ورود و خروج برای تعیین ورود توسط اپراتور باز می‌شود؛ UI ورود ساختگی ایجاد نمی‌کند. مجوز نهایی این اقدام را API تعیین می‌کند.
6. مدل خروج با `ExitDoorId`، شناسه تردد، عضو، خودرو، تعرفه، تصویر و کاربر ساخته می‌شود.
7. `TrafficApi.DoExitDump` فراخوانی می‌شود.
8. `ExitBillDto` دریافت می‌شود.
9. در صورت نیاز `PayForm` باز می‌شود.
10. پس از پرداخت یا صفر بودن مبلغ، چاپ فیش و فرمان راهبند اجرا می‌شود.
11. state فرم پاک می‌شود.

## ۸. قوانین کنترلی

### عضویت

- عضویت مسدود اجازه ادامه‌ی عادی ندارد.
- کارت و کد عضو به اطلاعات عضو و خودرو نگاشت می‌شوند.
- برای عضو چندخودرویی، انتخاب خودرو الزامی است.
- اعتبار جاری عضو از `TrafficApi.GetMemberCurrentCreditInfo` دریافت می‌شود.

### فهرست‌های کنترلی

- `Black`: عملیات متناظر متوقف و هشدار نمایش داده می‌شود.
- `Stealing`: بسته به نوع عملیات و درب، محدودیت اعمال می‌شود.
- `Observation`: هشدار نمایش داده می‌شود و الزاماً عملیات متوقف نمی‌گردد.

### تردد تکراری

در صورت فعال‌بودن گزینه، Grid جاری بررسی می‌شود و اگر ورود یا خروج مشابه در بازه مشخص وجود داشته باشد، عملیات متوقف می‌شود.

## ۹. APIهای فعلی و وضعیت پوشش

| endpoint از کد | کاربرد | وضعیت قرارداد |
|---|---|---|
| `ParkingApi.GetParkingDoor` | دریافت درب شیفت | نام endpoint مشخص؛ method و schema نیازمند تأیید |
| `ParkingApi.GetParkingDoorDevices` | دریافت تجهیزات درب | نام endpoint مشخص؛ schema ناقص |
| `ParkingApi.GetParkingEquipments` | دریافت تجهیزات پارکینگ | نام endpoint مشخص؛ اثر دقیق در فرم نیازمند بررسی |
| `TariffApi.GetByParkingId` | دریافت تعرفه‌ها | نام endpoint مشخص؛ schema ناقص |
| `TariffApi.AddMemberCar` | ثبت خودرو برای عضو | نام endpoint مشخص؛ قرارداد نیازمند تأیید |
| `TrafficApi.GetTraffics` | ترددهای اخیر درب | نام endpoint مشخص؛ pagination/method نامشخص |
| `TrafficApi.GetCarTrafficInfo` | یافتن وضعیت خودرو | نام endpoint مشخص؛ request در کد قابل استخراج است |
| `TrafficApi.CreateEnterDump` | ثبت ورود | نام endpoint مشخص؛ request قابل استخراج، response ناقص |
| `TrafficApi.DoExitDump` | ثبت خروج و تولید صورتحساب | نام endpoint مشخص؛ request قابل استخراج، response ناقص |
| `TrafficApi.GetMemberCurrentCreditInfo` | اعتبار عضو | نام endpoint مشخص؛ schema ناقص |
| `TrafficApi.GetTrafficBill` | اعتبارسنجی QR | نام endpoint مشخص؛ schema ناقص |
| `MemberApi.IsParkingMember` | بررسی عضویت | نام endpoint مشخص؛ استفاده دقیق نیازمند تکمیل |
| `ParkingApi.FindSimilarPlate` | یافتن پلاک مشابه | نام endpoint مشخص؛ schema ناقص |

علاوه بر endpointهای بالا، فرم در مسیر راهبند به‌صورت مستقیم از اتصال SQL محلی برای خواندن `Equipments` و درج در `OpenGateRequest` استفاده می‌کند. این مورد API فعلی محسوب نمی‌شود و باید در Windows Service یا Backend جایگزین شود.

### ۹.۱ مرز قرارداد API

از کدهای در scope فقط نام operationها، نوع helper کلاینت و DTOهای استفاده‌شده در این فرم قابل مشاهده است. route، method رسمی HTTP، header احراز هویت، schema قطعی پاسخ، status code و مجوز سمت Backend در این مستندات ادعا نمی‌شوند. تیم Backend باید قرارداد نسخه‌دار OpenAPI را تحویل دهد.

### ۹.۲ wrapper مشاهده‌شده در کلاینت

کد فرم نتیجه operationها را با فیلدهای `ResponseResultType`، `Message`، `RealMessage`، `Values` و در برخی مسیرها `HttpResponseType` بررسی می‌کند. این صرفاً رفتار مصرف‌کننده WinForms است؛ قرارداد UI React باید پس از تحویل error schema رسمی Backend نهایی شود.

### مدل‌های اصلی

مدل‌های مشاهده‌شده:

- `ParkingDoorEntity`
- `EquipmentEntity`
- `TariffDto`
- `TrafficDumpEntity`
- `CarTrafficInfoDto`
- `ExitBillDto`
- `EnterDumpRecordSavedDto`
- `MemberCreditDto`
- `DeviceInfoDto`

نگاشت ستون‌های Grid تردد به `ExitBillDto` شامل `MemberCode`، `CarPlateReversed`، `MemberFullName`، `EnterDateTime`، `ExitDateTime`، `PayType` و `CommonCost` است. ستون‌های پرداخت، خروج و بار command ایجاد می‌کنند و فیلد مستقل مدل نیستند. Grid مشخصات خودرو به `CardInfoView` و Grid وضعیت دستگاه به `DeviceInfoDto` متصل است.

فیلدهای مهم مدل‌های API که از source قابل استخراج‌اند:

- `CarTrafficInfoDto`: `Plate`، `CarId`، `CarName`، `CarModel`، `CarColor`، `MemberId`، `CardNumber`، `CardId`، `MemberCode`، `MemberFullName`، اطلاعات شخصی عضو، `ExitPermission`، `IsMemberBlock`، `CashAmount`، `MemberTariffId`، `MemberTariffName`، `EnterDateTime`، `ControlType`، `ControlListActionType`، `EnteranceImage`، `DumpId`، `BillControlType` و `MemberCars`.
- `ExitBillDto`: پلاک‌های قالب‌بندی‌شده، عضو و کارت، `DumpId`، `PayType`، `MemberCost`، `CommonCost`، `TotalCost`، مدت‌ها، زمان ورود/خروج، مجوز خروج، `TarrifIds`، `TarrifTitle`، `SourceType`، نوع خودرو، تصاویر ورود، درب ورود/خروج، مالیات، گردکردن و شناسه‌های قبض کنترلی.
- `EnterDumpRecordSavedDto`: حداقل `EnterDateTime` و شناسه پایه‌ی تردد.
- `DeviceInfoDto`: `DeviceId`، `DeviceName`، زمان اتصال و زمان دستگاه، زمان شمسی، نوع ترافیک، نوع دستگاه، نوع اتصال، firmware، ورودی‌بودن، پردازش سمت سرور، disabled و by-service.
- `MemberCreditDto`: کد عضو، موجودی، بازه اعتبار، فعال‌بودن، نوع اعتبار، عنوان و شناسه ثبت عضویت.
- `TariffDto`: عنوان، زمان‌ها، هزینه‌های انواع خودرو، حداقل زمان رایگان، قواعد rounding، وضعیت active/current، بازه‌ها، جزئیات شبانه و انواع عضویت.
- `TrafficDumpEntity` در request ورود/خروج: شناسه دستگاه، منبع، زمان‌ها، مجوز خروج، کاربر ثبت‌کننده، مبالغ، پرداخت، تعرفه، عضو، خودرو، کارت، درب ورود/خروج، نوع قبض، پلاک، نوع خودرو، تصویر و مدل/رنگ خودرو.

قرارداد رسمی فیلدها، nullable بودن، status codeها و خطاهای دامنه هنوز باید از Controller و DTO backend تأیید شود.

## ۱۰. خطاها و وضعیت‌های فعلی

| وضعیت | رفتار فعلی |
|---|---|
| loading | غیرفعال‌شدن موقت دکمه اصلی و نمایش انتظار محدود |
| empty | پاک‌شدن اطلاعات خودرو و غیرفعال‌شدن پنل عملیات |
| عضو پیدا نشد | پیام خطا و توقف ادامه عملیات |
| کارت پیدا نشد | پیام خطا و توقف ادامه عملیات |
| عضویت مسدود | پیام «عضویت غیر فعال است» |
| پلاک نامعتبر | توقف validation و درخواست اصلاح |
| تردد تکراری | پیام خطا و لغو state جاری |
| ورود ثبت نشده | جلوگیری از خروج، مگر گزینه استثنا فعال باشد |
| پرداخت ناموفق | راهبند باز نمی‌شود |
| اتصال قطع | رنگ وضعیت دستگاه و پیام خطا |
| خطای API | در بسیاری از مسیرها پیام عمومی؛ برخی exceptionها silent هستند |
| عدم مجوز | بیشتر به‌صورت hidden/disabled کنترل می‌شود و قرارداد یکپارچه ندارد |

### خطاهای فنی و رفتارهای پنهان فعلی

- بسیاری از exceptionها با `catch` خالی یا پیام عمومی پوشانده می‌شوند.
- دریافت ترددها با Thread جداگانه انجام می‌شود و خطای آن قرارداد قابل نمایش مستقلی ندارد.
- در `GetParkingDoorDevices` مسیرهای دریافت تجهیزات و برخی null-checkها شکننده‌اند و باید در Backend/Service بازطراحی شوند.
- بخشی از بازکردن راهبند مستقیماً از SQL به جدول `Equipments` یا `OpenGateRequest` متصل است.
- متد عمومی قدیمی `OpenGate(bool)` در ابتدای خود `return` دارد؛ بنابراین مسیر `DeviceManager.OpenGate` عملاً غیرفعال است و مسیرهای G4/Service/StPro جداگانه استفاده می‌شوند.
- فهرست جلوگیری از تکرار پلاک و کارت فقط روی کلاینت نگهداری می‌شود و مرجع قطعی جلوگیری از duplicate نیست.

## ۱۱. رفتارهایی که نباید مستقیماً به وب منتقل شوند

- اتصال TCP/IP به راهبند از Browser؛
- اتصال مستقیم Browser به کارت‌خوان، UHF، StPro یا G4؛
- اجرای Thread و `Thread.Sleep` برای کنترل workflow؛
- نگهداری state عملیاتی فقط در حافظه‌ی تب؛
- اعتماد به مقدار تعرفه یا مبلغ ارسالی از UI؛
- بازکردن راهبند صرفاً بر اساس موفقیت ظاهری UI؛
- ذخیره‌ی تنظیمات عملیاتی به‌صورت رشته‌ی comma-separated در مرورگر؛
- پنهان‌کردن خطا با `catch` خالی؛
- استفاده از اتصال مستقیم SQL برای فرمان راهبند از لایه UI.

## ۱۲. معماری پیشنهادی نسخه وب

### ۱۲.۱ تفکیک سه لایه

```text
React Web UI
    │ HTTPS / WebSocket
    ▼
Parking Backend API
    │ دامنه، مجوز، تعرفه، تردد، پرداخت، audit
    ▼
Windows Device Service
    │ اتصال TCP/IP، SDK و درایور دستگاه
    ▼
دوربین، کارت‌خوان، UHF، راهبند، POS
```

### ۱۲.۲ مسئولیت Web UI

UI وب فقط باید این موارد را انجام دهد:

- نمایش state رسمی سرور؛
- دریافت ورودی پلاک، کارت، کد عضو و انتخاب تعرفه؛
- ارسال commandهای دامنه‌ای مانند «استعلام خودرو»، «ثبت ورود» و «ثبت خروج»؛
- نمایش نتیجه، خطا و پیشرفت عملیات؛
- نمایش وضعیت تجهیزات دریافت‌شده از Backend/Service؛
- مدیریت route، مجوز نمایشی و accessibility.

UI نباید مستقیماً به IP تجهیزات، SQL یا SDK دستگاه دسترسی داشته باشد.

### ۱۲.۳ مسئولیت Backend API

Backend باید مرجع اصلی این موارد باشد:

- اعتبارسنجی نشست و مجوز؛
- تعیین پارکینگ، درب و شیفت جاری؛
- تعیین تعرفه معتبر؛
- یافتن عضو، خودرو و تردد باز؛
- کنترل فهرست‌های کنترلی؛
- ثبت اتمیک ورود یا خروج؛
- محاسبه مبلغ و وضعیت پرداخت؛
- صدور فرمان عملیاتی برای Service؛
- ثبت audit برای ورود، خروج، پرداخت و بازشدن راهبند؛
- جلوگیری از duplicate command و replay؛
- تبدیل خطاهای Service به خطای قابل فهم برای UI.

### ۱۲.۴ مسئولیت Windows Device Service

سرویس ویندوزی باید در شبکه پارکینگ نصب شود و مسئول این موارد باشد:

- اتصال و احراز هویت با تجهیزات؛
- نگهداری connection pool و reconnect؛
- دریافت event کارت، UHF و رکورد دستگاه؛
- دریافت تصویر یا trigger دوربین؛
- نگهداری credential دوربین و ارتباط با camera/plate detector؛
- ارسال فرمان بازکردن راهبند؛
- تنظیم ساعت تجهیزات؛
- پایش heartbeat و health؛
- صف‌کردن فرمان‌ها در قطعی موقت Backend؛
- گزارش نتیجه‌ی واقعی فرمان، نه صرفاً ارسال درخواست؛
- دریافت پاسخ POS و تبدیل آن به نتیجه پرداخت؛
- جلوگیری از بازکردن تکراری راهبند؛
- ثبت log و correlation id.

### ۱۲.۵ مرز مسئولیت سرویس تجهیزات در نسخه وب

کد WinForms موجود ارتباط مستقیم با بخشی از تجهیزات را نشان می‌دهد، اما سورس سرویس واسط یا Backend در scope مستندات نیست. **تصمیم محصول:** Browser مستقیم به تجهیز وصل نمی‌شود؛ تیم Backend باید معماری Agent/Windows Service و قرارداد command/event را طراحی و تحویل دهد. UI فقط API و event تأییدشده را مصرف می‌کند.

## ۱۳. قرارداد پیشنهادی API وب

### context

```http
GET /api/v1/parking-context
```

خروجی پیشنهادی:

```json
{
  "parkingId": 1,
  "doorId": 10,
  "doorType": "Both",
  "entryAuthorizationType": "CardAndPlate",
  "permissions": ["traffic.enter", "traffic.exit", "traffic.pay"],
  "tariffs": [],
  "devices": []
}
```

### استعلام خودرو

```http
POST /api/v1/traffic/lookup
```

```json
{
  "parkingId": 1,
  "doorId": 10,
  "plate": "...،...،...",
  "cardNumber": "...",
  "memberCode": "...",
  "source": "manual"
}
```

Backend باید نتیجه‌ی نهایی قابل مصرف UI را برگرداند:

- خودرو و عضو؛
- تردد باز؛
- وضعیت عضویت؛
- تعرفه؛
- اعتبار؛
- وضعیت فهرست کنترلی؛
- عملیات مجاز؛
- نیاز یا عدم نیاز به انتخاب خودرو، پرداخت یا قبض اولیه.

### ثبت ورود

```http
POST /api/v1/traffic/entries
```

درخواست باید شامل `doorId`، شناسه خودرو/عضو، پلاک، کارت، تعرفه انتخابی، نوع منبع، تصویر و گزینه قبض کنترلی باشد. Backend باید تعرفه، مجوز، تکراری‌بودن و فهرست کنترلی را دوباره بررسی کند.

### ثبت خروج

```http
POST /api/v1/traffic/exits
```

Backend باید صورتحساب، نیاز پرداخت، قبض اولیه، تصویر خروج و وضعیت راهبند را مدیریت کند. UI نباید مبلغ را محاسبه یا موفقیت خروج را قبل از پاسخ معتبر سرور اعلام کند.

### پرداخت

```http
POST /api/v1/traffic/{trafficId}/payments
```

پرداخت باید idempotency key داشته باشد و نتیجه‌ی نهایی آن قابل پیگیری باشد.

### ترددهای جاری

```http
GET /api/v1/traffic?doorId=10&status=open&page=1&pageSize=50
```

برای بروزرسانی، WebSocket یا Server-Sent Events بر polling ساده اولویت دارد؛ polling باید fallback باشد.

## ۱۴. API ارتباط Backend با Windows Service

دو مدل قابل استفاده است:

### مدل پیشنهادی: Service به Backend متصل شود

سرویس ویندوزی یک اتصال خروجی امن به Backend برقرار می‌کند:

- `SignalR` یا WebSocket برای event و command؛
- TLS و credential مخصوص service؛
- heartbeat؛
- correlation id؛
- acknowledgement دو مرحله‌ای: دریافت فرمان و نتیجه واقعی دستگاه.

مزیت این مدل، نبود نیاز به exposeکردن سرویس ویندوزی در اینترنت است.

### مدل جایگزین: Backend به Service متصل شود

در شبکه امن پارکینگ، Backend می‌تواند به endpoint سرویس متصل شود:

```http
POST http://device-service.local/api/v1/devices/{deviceId}/commands/open-gate
POST http://device-service.local/api/v1/devices/{deviceId}/commands/set-time
GET  http://device-service.local/api/v1/devices/health
```

این مدل فقط در صورت VPN، mTLS، allow-list شبکه و احراز هویت service قابل قبول است.

### event تجهیزات

```json
{
  "eventId": "uuid",
  "serviceId": "service-1",
  "deviceId": 20,
  "eventType": "card.detected",
  "occurredAt": "2026-09-10T10:00:00Z",
  "payload": { "cardNumber": "..." }
}
```

Backend event را به context پارکینگ و درب مرتبط می‌کند و فقط event مجاز را به sessionهای UI همان پارکینگ ارسال می‌کند.

### فرمان بازکردن راهبند

```json
{
  "commandId": "uuid",
  "doorId": 10,
  "deviceId": 20,
  "relay": 1,
  "reason": "traffic.entry",
  "trafficId": 500,
  "requestedBy": 7,
  "expiresAt": "2026-09-10T10:00:10Z"
}
```

نتیجه باید حداقل یکی از این وضعیت‌ها را داشته باشد:

- `accepted`
- `sent`
- `device_acknowledged`
- `opened`
- `failed`
- `timeout`
- `cancelled`

## ۱۵. realtime و همگام‌سازی UI وب

پس از ورود کاربر، UI باید به کانال محدود به `parkingId` و `doorId` متصل شود. eventهای قابل ارسال:

- `traffic.updated`
- `plate.detected`
- `card.detected`
- `device.status_changed`
- `gate.command_status_changed`
- `payment.updated`
- `session.revoked`

اگر اتصال realtime قطع شد:

1. UI وضعیت disconnected نمایش دهد.
2. عملیات حساس جدید تا دریافت context معتبر محدود شود.
3. از polling با backoff استفاده شود.
4. پس از اتصال مجدد، snapshot کامل context و ترددها دریافت شود.

تنظیمات محلی فرم ویندوزی در کلید `Enter&ExitType` و به‌صورت رشته‌ی comma-separated ذخیره می‌شوند؛ این روش نباید قرارداد ذخیره‌سازی نسخه وب باشد. در وب، تنظیمات باید با schema نسخه‌دار و scope مشخص کاربر/درب/پارکینگ ذخیره شوند.

## ۱۶. session، retry و cancellation در نسخه وب

- هر عملیات ورود، خروج، پرداخت و راهبند باید `operationId` و `idempotencyKey` داشته باشد.
- retry فقط برای خطای شبکه یا timeout مجاز است؛ ثبت دوباره‌ی تردد بدون idempotency ممنوع است.
- cancellation در UI فقط باید command در حال انتظار را لغو کند؛ اگر دستگاه فرمان را اجرا کرده باشد، لغو ظاهری نباید نتیجه را معکوس اعلام کند.
- خروج از صفحه نباید عملیات ثبت‌شده را متوقف یا ناموفق تلقی کند.
- انقضای session باید با پیام واضح و توقف عملیات جدید مدیریت شود.
- تغییر پارکینگ یا درب باید تمام subscriptionهای قبلی را ببندد.

## ۱۷. audit و امنیت

برای هر عملیات حساس باید ثبت شود:

- کاربر؛
- پارکینگ و درب؛
- دستگاه؛
- زمان درخواست و زمان اجرا؛
- شناسه تردد؛
- command id؛
- نتیجه واقعی دستگاه؛
- مبلغ و روش پرداخت؛
- دلیل استثنا مانند خروج بدون ورود؛
- IP یا شناسه سرویس؛
- correlation id.

تصویر پلاک و اطلاعات شخصی عضو باید با سطح دسترسی و retention policy کنترل شود. **تصمیم محصول:** تصویر زنده تا حد امکان از خود دوربین نمایش داده شود، در قطع ارتباط reconnect خودکار با backoff داشته باشد و UI فقط buffer محدود نگه دارد و هنگام خروج/تغییر درب آن را آزاد کند. retention، محل ذخیره‌سازی، حذف، کیفیت/حجم، reconnect و permissionها باید در تنظیمات schema‌دار پارکینگ/درب قابل کنترل و در Backend enforce شوند. credential دستگاه نباید به Browser یا JavaScript ارسال شود.

## ۱۸. موارد باز و نیازمند backend

| مورد | وضعیت |
|---|---|
| method رسمی endpointها | نیازمند بررسی Controller/API client |
| schema کامل DTOها | نیازمند بررسی backend |
| status code و کد خطا | نیازمند قرارداد رسمی |
| محاسبه مبلغ و rounding | تصمیم محصول: کاملاً در Backend محاسبه می‌شود؛ UI فقط ورودی‌های لازم را ارسال و نتیجه را نمایش می‌دهد |
| تراکنش اتمیک ثبت خروج/پرداخت/فرمان راهبند | نیازمند تصمیم معماری |
| رفتار قطعی خروج بدون ورود | تصمیم محصول ثبت شد: انتقال به مدیریت ورود/خروج برای تعیین ورود؛ authorization نهایی با API |
| رفتار چند درب و چند سرویس دستگاه | نیازمند طراحی topology |
| ذخیره موقت eventهای قطع شبکه | نیازمند تصمیم درباره offline mode |
| منبع تصویر پلاک‌خوان | نیازمند انتخاب SDK/service |
| مجوزهای رسمی عملیات | نیازمند رجیستری نام‌دار |
| قرارداد پرداخت POS | نیازمند بررسی `PcPosManager` و backend |
| قرارداد ثبت بار | نیازمند تکمیل API فرم بار |

## ۱۹. نتیجه معماری

در نسخه وب، `ManualTrafficControlForm` نباید به‌صورت انتقال مستقیم WinForms بازسازی شود. پیشنهاد می‌شود به یک صفحه عملیاتی با componentهای مستقل تبدیل شود:

- `TrafficIdentityPanel`
- `MemberAndVehiclePanel`
- `TrafficActionPanel`
- `TrafficHistoryGrid`
- `DeviceHealthPanel`
- `CameraPanel`
- `PaymentPanel`
- `ControlListAlert`

Backend مرجع تصمیم‌های کسب‌وکار و Windows Service مرجع ارتباط با سخت‌افزار است. UI وب فقط state رسمی، نتیجه عملیات و eventهای مجاز را نمایش می‌دهد.
